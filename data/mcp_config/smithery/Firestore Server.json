{
    "mcpServers": {
        "firestore-mcp": {
            "command": "npx",
            "args": [
                "-y",
                "@smithery/cli@latest",
                "run",
                "@devlimelabs/firestore-mcp",
                "--key",
                "your key"
            ],
            "mcp_list_name": "Firestore Server",
            "mcp_list_description": "Provide secure, permission-controlled access to Firebase Firestore databases through a standardized MCP interface. Enable AI assistants and other clients to perform full CRUD operations, advanced queries, batch operations, and transactional updates with granular collection and operation-level permissions. Ensure data consistency and security with default deny rules and fine-grained access control."
        }
    }
}