[
    {
        "name": "mcp-server-motherduck_query",
        "description": "Use this to execute a query on the MotherDuck or DuckDB database",
        "parameters": {
            "type": "object",
            "properties": {
                "query": {
                    "type": "string",
                    "description": "SQL query to execute that is a dialect of DuckDB SQL"
                }
            },
            "required": [
                "query"
            ]
        }
    }
]