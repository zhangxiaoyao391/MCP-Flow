[
    {
        "name": "yourware-mcp_check_credentials",
        "description": "Check your yourware credentials exists and are valid.",
        "parameters": {
            "properties": {},
            "title": "check_credentialsArguments",
            "type": "object"
        }
    },
    {
        "name": "yourware-mcp_create_api_key",
        "description": "Create a new yourware API key. This will automatically be stored in /Users/bytedance/.yourware/credentials.json. Use this tool if current credentials are invalid",
        "parameters": {
            "properties": {
                "api_key": {
                    "anyOf": [
                        {
                            "type": "string"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "default": null,
                    "title": "Api Key"
                }
            },
            "title": "create_api_keyArguments",
            "type": "object"
        }
    },
    {
        "name": "yourware-mcp_upload_project",
        "description": "Upload a file or directory to yourware, might be a dist/out directory or a single html file. Use absolute path if possible. For multiple files, you should move them to a directory first, then use this tool to upload the directory",
        "parameters": {
            "properties": {
                "file_path": {
                    "title": "File Path",
                    "type": "string"
                },
                "cwd": {
                    "anyOf": [
                        {
                            "type": "string"
                        },
                        {
                            "type": "null"
                        }
                    ],
                    "default": null,
                    "title": "Cwd"
                }
            },
            "required": [
                "file_path"
            ],
            "title": "upload_projectArguments",
            "type": "object"
        }
    }
]