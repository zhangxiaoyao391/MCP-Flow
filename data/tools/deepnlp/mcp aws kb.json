[
    {
        "name": "build-vault_list_products",
        "description": "List AI products from vault.buildaipod.com with optional filtering",
        "parameters": {
            "type": "object",
            "properties": {
                "limit": {
                    "type": "number",
                    "description": "Number of products to return (max 100)",
                    "default": 20
                },
                "offset": {
                    "type": "number",
                    "description": "Offset for pagination",
                    "default": 0
                },
                "speaker": {
                    "type": "string",
                    "description": "Filter by speaker name"
                },
                "episode_id": {
                    "type": "string",
                    "description": "Filter by episode ID"
                }
            }
        }
    },
    {
        "name": "build-vault_search_products",
        "description": "Semantic search for AI products using natural language",
        "parameters": {
            "type": "object",
            "properties": {
                "query": {
                    "type": "string",
                    "description": "Natural language search query"
                },
                "limit": {
                    "type": "number",
                    "description": "Number of results to return (max 50)",
                    "default": 10
                },
                "similarity_threshold": {
                    "type": "number",
                    "description": "Minimum similarity score (0-1)",
                    "default": 0.7
                }
            },
            "required": [
                "query"
            ]
        }
    },
    {
        "name": "build-vault_get_product_details",
        "description": "Get detailed information about a specific AI product",
        "parameters": {
            "type": "object",
            "properties": {
                "product_id": {
                    "type": "string",
                    "description": "The ID of the product"
                }
            },
            "required": [
                "product_id"
            ]
        }
    },
    {
        "name": "build-vault_find_similar_products",
        "description": "Find products similar to a given product using vector similarity",
        "parameters": {
            "type": "object",
            "properties": {
                "product_id": {
                    "type": "string",
                    "description": "The ID of the reference product"
                },
                "limit": {
                    "type": "number",
                    "description": "Number of similar products to return",
                    "default": 5
                },
                "similarity_threshold": {
                    "type": "number",
                    "description": "Minimum similarity score (0-1)",
                    "default": 0.8
                }
            },
            "required": [
                "product_id"
            ]
        }
    },
    {
        "name": "build-vault_search_by_date_range",
        "description": "Find AI products within a specific date range",
        "parameters": {
            "type": "object",
            "properties": {
                "start_date": {
                    "type": "string",
                    "description": "Start date (YYYY-MM-DD format)"
                },
                "end_date": {
                    "type": "string",
                    "description": "End date (YYYY-MM-DD format, optional)"
                },
                "limit": {
                    "type": "number",
                    "description": "Number of results to return",
                    "default": 50
                }
            },
            "required": [
                "start_date"
            ]
        }
    },
    {
        "name": "build-vault_search_by_category",
        "description": "Search AI products by category",
        "parameters": {
            "type": "object",
            "properties": {
                "category": {
                    "type": "string",
                    "description": "Category to search (e.g., 'business_ideas', 'frameworks_and_exercises', 'products')"
                },
                "limit": {
                    "type": "number",
                    "description": "Number of results to return",
                    "default": 20
                }
            },
            "required": [
                "category"
            ]
        }
    },
    {
        "name": "build-vault_search_by_timeframe",
        "description": "Search AI products within a specific time range",
        "parameters": {
            "type": "object",
            "properties": {
                "start_timestamp": {
                    "type": "number",
                    "description": "Start timestamp"
                },
                "end_timestamp": {
                    "type": "number",
                    "description": "End timestamp"
                },
                "episode_id": {
                    "type": "string",
                    "description": "Optional episode ID to filter by"
                },
                "limit": {
                    "type": "number",
                    "description": "Number of results to return",
                    "default": 20
                }
            }
        }
    },
    {
        "name": "build-vault_get_timeline_insights",
        "description": "Get chronological insights across episodes",
        "parameters": {
            "type": "object",
            "properties": {
                "episode_id": {
                    "type": "string",
                    "description": "Optional episode ID to filter by"
                },
                "limit": {
                    "type": "number",
                    "description": "Number of insights to return",
                    "default": 50
                }
            }
        }
    },
    {
        "name": "build-vault_search",
        "description": "Search AI insights, episodes, speakers, and topics from Build AI Pod vault for deep research",
        "parameters": {
            "type": "object",
            "properties": {
                "query": {
                    "type": "string",
                    "description": "Natural language search query (e.g., 'insights about AI agents', 'what did Sam Altman say about AGI', 'episodes on prompt engineering')"
                }
            },
            "required": [
                "query"
            ]
        }
    },
    {
        "name": "build-vault_fetch",
        "description": "Retrieve complete content with full context and metadata for deep research and citation",
        "parameters": {
            "type": "object",
            "properties": {
                "id": {
                    "type": "string",
                    "description": "Content ID (insight, episode, or other resource)"
                }
            },
            "required": [
                "id"
            ]
        }
    }
]