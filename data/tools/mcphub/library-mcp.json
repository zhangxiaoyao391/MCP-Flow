[
    {
        "name": "get_by_tag",
        "description": "Get blog content by its tag.\n\nArgs:\n    tag: the tag associated with content\n    limit: the number of results to include\n",
        "parameters": {
            "properties": {
                "tag": {
                    "title": "Tag",
                    "type": "string"
                },
                "limit": {
                    "default": 50,
                    "title": "Limit",
                    "type": "integer"
                }
            },
            "required": [
                "tag"
            ],
            "title": "get_by_tagArguments",
            "type": "object"
        }
    },
    {
        "name": "get_by_text",
        "description": "Get blog content by text in content.\n\nArgs:\n    query: text for an exact match\n    limit: the number of results to include\n",
        "parameters": {
            "properties": {
                "query": {
                    "title": "Query",
                    "type": "string"
                },
                "limit": {
                    "default": 50,
                    "title": "Limit",
                    "type": "integer"
                }
            },
            "required": [
                "query"
            ],
            "title": "get_by_textArguments",
            "type": "object"
        }
    },
    {
        "name": "rebuild",
        "description": "Rebuild text index. Useful for when contents have changed on disk",
        "parameters": {
            "properties": {},
            "title": "rebuildArguments",
            "type": "object"
        }
    },
    {
        "name": "search_tags",
        "description": "Search for tags matching the provided query.\n\nArgs:\n    tag_query: partial or full tag name to search for\n    limit: the maximum number of tags to return\n",
        "parameters": {
            "properties": {
                "tag_query": {
                    "title": "Tag Query",
                    "type": "string"
                },
                "limit": {
                    "default": 20,
                    "title": "Limit",
                    "type": "integer"
                }
            },
            "required": [
                "tag_query"
            ],
            "title": "search_tagsArguments",
            "type": "object"
        }
    },
    {
        "name": "list_all_tags",
        "description": "List all tags sorted by number of posts and most recent post.",
        "parameters": {
            "properties": {},
            "title": "list_all_tagsArguments",
            "type": "object"
        }
    },
    {
        "name": "get_by_slug_or_url",
        "description": "Get a post by its slug or URL.\n\nArgs:\n    identifier: the slug, URL, or path fragment to search for\n",
        "parameters": {
            "properties": {
                "identifier": {
                    "title": "Identifier",
                    "type": "string"
                }
            },
            "required": [
                "identifier"
            ],
            "title": "get_by_slug_or_urlArguments",
            "type": "object"
        }
    },
    {
        "name": "get_by_date_range",
        "description": "Get posts published within a date range.\n\nArgs:\n    start_date: the start date in ISO format (YYYY-MM-DD)\n    end_date: the end date in ISO format (YYYY-MM-DD)\n    limit: the maximum number of posts to return\n",
        "parameters": {
            "properties": {
                "start_date": {
                    "title": "Start Date",
                    "type": "string"
                },
                "end_date": {
                    "title": "End Date",
                    "type": "string"
                },
                "limit": {
                    "default": 50,
                    "title": "Limit",
                    "type": "integer"
                }
            },
            "required": [
                "start_date",
                "end_date"
            ],
            "title": "get_by_date_rangeArguments",
            "type": "object"
        }
    }
]