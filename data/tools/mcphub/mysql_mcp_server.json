[
    {
        "name": "execute_sql",
        "description": "Execute an SQL query on the MySQL server",
        "parameters": {
            "type": "object",
            "properties": {
                "query": {
                    "type": "string",
                    "description": "The SQL query to execute"
                }
            },
            "required": [
                "query"
            ]
        }
    }
]