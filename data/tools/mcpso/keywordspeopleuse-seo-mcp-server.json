[
    {
        "name": "keywordspeopleuse_people-also-ask",
        "description": "Get People Also Ask questions for a given query",
        "parameters": {
            "type": "object",
            "properties": {
                "q": {
                    "type": "string",
                    "minLength": 2,
                    "description": "A query to use for getting People Also Ask questions"
                },
                "hl": {
                    "type": "string",
                    "minLength": 2,
                    "maxLength": 2,
                    "default": "en",
                    "description": "Two-letter language code (e.g. en, fr, es, de)"
                },
                "gl": {
                    "type": "string",
                    "minLength": 2,
                    "maxLength": 2,
                    "default": "us",
                    "description": "Two-letter country code (e.g. us, uk, ca, in, au)"
                }
            },
            "required": [
                "q"
            ],
            "additionalProperties": false,
            "$schema": "http://json-schema.org/draft-07/schema#"
        }
    },
    {
        "name": "keywordspeopleuse_google-autocomplete",
        "description": "Get Google Autocomplete suggestions for a given query",
        "parameters": {
            "type": "object",
            "properties": {
                "q": {
                    "type": "string",
                    "minLength": 2,
                    "description": "A query to use for getting Google Autocomplete suggestions"
                },
                "hl": {
                    "type": "string",
                    "minLength": 2,
                    "maxLength": 2,
                    "default": "en",
                    "description": "Two-letter language code (e.g. en, fr, es, de)"
                },
                "gl": {
                    "type": "string",
                    "minLength": 2,
                    "maxLength": 2,
                    "default": "us",
                    "description": "Two-letter country code (e.g. us, uk, ca, in, au)"
                }
            },
            "required": [
                "q"
            ],
            "additionalProperties": false,
            "$schema": "http://json-schema.org/draft-07/schema#"
        }
    },
    {
        "name": "keywordspeopleuse_forums",
        "description": "Get Reddit and Quora questions for a given query",
        "parameters": {
            "type": "object",
            "properties": {
                "q": {
                    "type": "string",
                    "minLength": 2,
                    "description": "A query to use for getting Reddit and Quora questions"
                },
                "hl": {
                    "type": "string",
                    "minLength": 2,
                    "maxLength": 2,
                    "default": "en",
                    "description": "Two-letter language code (e.g. en, fr, es, de)"
                },
                "gl": {
                    "type": "string",
                    "minLength": 2,
                    "maxLength": 2,
                    "default": "us",
                    "description": "Two-letter country code (e.g. us, uk, ca, in, au)"
                }
            },
            "required": [
                "q"
            ],
            "additionalProperties": false,
            "$schema": "http://json-schema.org/draft-07/schema#"
        }
    },
    {
        "name": "keywordspeopleuse_semantic-keywords",
        "description": "Get Semantic (similar, related) keywords for a given query",
        "parameters": {
            "type": "object",
            "properties": {
                "q": {
                    "type": "string",
                    "minLength": 2,
                    "description": "A query to use for getting semantic keywords"
                },
                "hl": {
                    "type": "string",
                    "minLength": 2,
                    "maxLength": 2,
                    "default": "en",
                    "description": "Two-letter language code (e.g. en, fr, es, de)"
                },
                "gl": {
                    "type": "string",
                    "minLength": 2,
                    "maxLength": 2,
                    "default": "us",
                    "description": "Two-letter country code (e.g. us, uk, ca, in, au)"
                }
            },
            "required": [
                "q"
            ],
            "additionalProperties": false,
            "$schema": "http://json-schema.org/draft-07/schema#"
        }
    }
]