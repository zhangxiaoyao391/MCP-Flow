[
    {
        "name": "mcp-status-observer_status",
        "description": "Check operational status of major digital platforms",
        "parameters": {
            "type": "object",
            "properties": {
                "command": {
                    "type": "string",
                    "description": "Command to execute (list, --all, or platform with -- prefix like --github)"
                }
            },
            "required": [
                "command"
            ]
        }
    }
]