{
    "mcpServers": {
        "postgresql-mcp-server": {
            "command": "npx",
            "args": [
                "-y",
                "@smithery/cli@latest",
                "run",
                "@1Levick3/postgresql-mcp-server",
                "--key",
                "your key"
            ],
            "mcp_list_name": "PostgreSQL Database Query Server",
            "mcp_list_description": "Enable direct execution of custom SQL queries against PostgreSQL databases with support for parameterized queries and configurable timeouts. Securely manage database connections with SSL/TLS and connection pooling. Simplify database management and monitoring through an MCP-compliant interface."
        }
    }
}